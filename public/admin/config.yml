backend:
  name: github
  repo: твой-логин/название-репозитория # Путь к репозиторию на GitHub
  branch: main
  base_url: https://your-oauth-proxy.com # Ссылка на прокси (разберем ниже)

media_folder: "public/assets/blog" # куда сохранять картинки
public_folder: "/assets/blog"

collections:
  - name: "blog"
    label: "Блог"
    folder: "src/content/blog"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - { label: "Заголовок", name: "title", widget: "string" }
      - { label: "Дата", name: "date", widget: "datetime" }
      - { label: "Обложка", name: "image", widget: "image" }
      - { label: "Описание (SEO)", name: "description", widget: "text" }
      - { label: "Текст поста", name: "body", widget: "markdown" }